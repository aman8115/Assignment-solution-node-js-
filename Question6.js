const os = require("os")
// find operating system name
const opeSystem = os.type()
console.log(" Operating System Name : " + opeSystem)
// os release
const relea = os.release()
console.log( " Os release :" +  relea)